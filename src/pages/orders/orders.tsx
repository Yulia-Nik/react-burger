import ProfileSidebar from '../../components/profile-sidebar/profile-sidebar';

const Orders = (): JSX.Element => {
	return (
		<>
			<ProfileSidebar />
			<div>Здесь будет история заказов</div>
		</>
	);
};

export default Orders;
